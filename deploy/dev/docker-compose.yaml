services:
  oxalate-paradedb:
    image: paradedb/paradedb:latest
    container_name: harvester-postgres-db
    #   POSTGRES_USER: ${POSTGRES_USER:-root}
    #   POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-root}
    #   POSTGRES_DB: ${POSTGRES_DB:-Oxalate}
    env_file:
      - ../../.env.dev
    environment:
      POSTGRES_DB: ${POSTGRES_NAME}
    volumes:
      - oxalate_paradedb_data:/var/lib/postgresql
    labels:
      kompose.init.containers.command: "echo Initializing harvester postgres database"
      kompose.controller.expose.port: true
    ports:
      - "6666:5432"
    networks:
      - app-network
    restart: unless-stopped

  oxalate-redpanda:
    image: docker.redpanda.com/redpandadata/redpanda:latest
    command:
      - redpanda
      - start
      - --smp 1                              
      - --mode dev-container
      - --default-log-level=info
      - --kafka-addr internal://0.0.0.0:9092,external://0.0.0.0:19092
      - --advertise-kafka-addr internal://redpanda-0:9092,external://localhost:19092
      - --pandaproxy-addr internal://0.0.0.0:8082,external://0.0.0.0:18082
      - --rpc-addr 0.0.0.0:33145
    ports:
      - "18081:18081"     # Schema Registry
      - "18082:18082"     # HTTP Proxy
      - "19092:19092"     # Kafka (external)
      - "19644:9644"      # Admin API / Prometheus
    volumes:
      - oxalate_redpanda_data:/var/lib/redpanda/data
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  oxalate_paradedb_data:
  oxalate_redpanda_data:

# postgresql://root:root@localhost:6666/Oxalate
